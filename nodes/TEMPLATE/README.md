# Node Template

Copy this folder to create a new node:
- Choose a new ID (e.g., N010)
- Rename the folder to `nodes/<ID>_<short_name>/`
- Update `node.yaml` meta.id/title/status and depends_on
- Fill narrative + math + latex + code as needed

Keep nodes small and dependency-driven.
